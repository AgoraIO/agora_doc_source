# This workflow will use OxygenScript to build features.

name: Awesome OxygenScript for DITA wellformedness check

on: [pull_request]

jobs:
  build:
  
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'temurin'
    - name: DITA wellformedness check
      run: |
          cd scripts/oxygen/scripts
          ./validateCheckDITA.sh -i ../../../dita/rtc_api.xpr -r ditaValidationReport_cn.xml || cat ditaValidationReport_cn.xml
          ./validateCheckDITA.sh -i ../../../en-US/dita/rtc_api.xpr -r ditaValidationReport_en.xml || cat ditaValidationReport_en.xml
