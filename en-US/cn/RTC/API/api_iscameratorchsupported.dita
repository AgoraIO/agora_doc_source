<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE reference PUBLIC "-//OASIS//DTD DITA Reference//EN" "reference.dtd">
<reference id="api_iscameratorchsupported">
    <title><ph keyref="isCameraTorchSupported"/></title>
    <shortdesc id="short"><ph id="shortdesc">Check whether the device supports the flash always on.</ph></shortdesc>
    <prolog>
        <metadata>
   <keywords>
       <indexterm keyref="isCameraTorchSupported" />
   </keywords>
        </metadata>
    </prolog>
    <refbody><section id="prototype">
        
        <p conref="../conref/conref_rtc_api.dita#apidef/isCameraTorchSupported"/>
        </section>
        <section id="detailed_desc">
   
   <p>This method needs to be called after the camera is started (for example, by calling <xref keyref="startPreview"/> or <xref keyref="joinChannel1"/>).</p>
   <note type="attention">In general, the app turns on the front camera by default, so if your front camera does not support the flash always on, using this method directly will return false. If you need to check whether the rear camera supports the flash always on, you need to use the<xref keyref="switchCamera"/> switch camera first, and then use this method.</note>
        </section>
        <section id="return_values">
   <title>Returns</title>
   <ul>
       <li><ph keyref="true"/>: The device supports the flash always on.</li>
       <li><ph keyref="false"/>: The device does not support the flash always on.</li>
   </ul>
        </section></refbody>
</reference>
